---
title: "Summarising_VCFs"
output: html_document
date: "2025-08-29"
---

```{r}
# Setup ----------------------------------------------------------
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# Libraries ------------------------------------------------------
library(ggplot2)
library(dplyr)
library(stringr)
library(patchwork)
```

```{r Setting Paths}
R_Data <- "/home/ocdm0351/DPhil/R_Data/" # For HPC
#R_Data <- "/home/dan/DPhil/MA_Project_1/R_Data/" # For Laptop
Species_Data <- "/home/ocdm0351/.conda/envs/snpEff/share/snpeff-5.2-2/data/" # For HPC
#Species_Data <- "/home/dan/DPhil/MA_Project_1/R_Data/data/" # For Laptop

source(paste0(R_Data, "plotting.themes.R"))
# Choose theme once per session/script
set_my_theme("light")   # or "dark"
```

```{r}
# Load Annotated VCFs -------------------------------------------
load(file = paste0(R_Data, "Annotated_VCFs_Raw"))

# Counting Unique Variant Loci per Dataset -----------------------
Unique_Variant_Loci <- lapply(
  Annotated_VCFs_Raw,
  function(df) df[, 1:2] %>% unique()
)

# Create and save plots ------------------------------------------
List_of_Unique_Variant_Loci_Plots <- lapply(names(Unique_Variant_Loci), function(element_name) {

  element <- Unique_Variant_Loci[[element_name]]
  build   <- str_split(element_name, "_", simplify = TRUE)[, 5]
  species <- str_split(element_name, "_", simplify = TRUE)[, 4] %>%
    str_replace(., "^(.)(.*)$", "\\1_\\2")
  speciesANDbuild <- paste(species, build, sep = "_")

  counts <- element %>% count(CHROM)

  plot <- ggplot2::ggplot(counts, aes(x = reorder(CHROM, n), y = n)) +
    geom_bar(stat = "identity", fill = "orange", colour = "black") +
    labs(
      title = element_name,
      x = "Chromosome",
      y = "Unique Variants"
    ) +
    theme(
      axis.text  = element_text(size = 20),
    )

  # Save each plot individually
  ggsave(
    plot,
    filename = paste0(R_Data, "Plots/1_Summaries/Unique_Variants_per_Chromosome_",
      element_name, ".png"
    ),
    type = "cairo",
    units = "cm",
    width = ifelse(
      length(unique(counts$CHROM)) < 26,
      length(unique(counts$CHROM)) + 4,
      26 + 4
    ),
    height = 7,
    create.dir = TRUE,
    dpi = 2000
  )

  return(plot)
})


# Counting Unique Variants per Dataset ---------------------------
Unique_Variants <- lapply(
  Annotated_VCFs_Raw,
  function(df) df[, 1:4] %>% unique()
)

List_of_Unique_Variants_Plots <- lapply(names(Unique_Variants), function(element_name) {

  element <- Unique_Variants[[element_name]]
  element$variantType <- paste(element$REF, element$ALT, sep = ":")

  build <- str_split(element_name, "_", simplify = TRUE)[, 5]
  species <- str_split(element_name, "_", simplify = TRUE)[, 4] %>%
    str_replace(., "^(.)(.*)$", "\\1_\\2")
  speciesANDbuild <- paste(species, build, sep = "_")

  counts <- element %>% count(variantType)
  top_counts <- dplyr::top_n(counts, 30, n)

  plot <- ggplot2::ggplot(top_counts, aes(y = reorder(variantType, n), x = n)) +
    geom_bar(stat = "identity", fill = "orange", colour = "black") +
    theme(axis.text = element_text(size = 20)) +
    labs(
      title = paste(element_name, "\n", "Top", nrow(top_counts), "Common Variants.\n",
                    nrow(counts), "Unique Variants in Total."),
      y = NULL,
      x = "Count"
    )

  ggsave(
    filename = paste0(
      R_Data, "/Plots/1_Summaries/Unique_Variants_",
      element_name, ".png"
    ),
    plot = plot,
    type = "cairo",
    units = "cm",
    height = 20,
    width = 20,
    dpi = 2000
  )

  plot
})
```
